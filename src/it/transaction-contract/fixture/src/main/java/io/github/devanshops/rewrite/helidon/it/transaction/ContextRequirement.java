package io.github.devanshops.rewrite.helidon.it.transaction;

/** Transaction context rule whose provider-specific rejection is being classified. */
public enum ContextRequirement {
    MANDATORY,
    NEVER
}
