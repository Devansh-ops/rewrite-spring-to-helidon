# Preserve public recipe compatibility through v0.2

Version 0.2 preserves the existing public recipe identifiers and the canonical migration's
conservative default behavior. New support may widen a documented bounded migration, but a
previous refusal changes into an automatic edit only with explicit documentation, public-interface
tests, and compatibility review; architecture-dependent behavior remains separately activatable.
